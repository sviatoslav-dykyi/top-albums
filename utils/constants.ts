/**
 * Sizes
 */

export const OUTSIDE = 16;

/**
 * Username
 */

export const USER_NAME_DEFAULT = 'rj';

/**
 * Images
 */

const imagePath = "../assets/images/";

export const Images = {
  NoImage: require(`${imagePath}no-image.png`),
  Play: require(`${imagePath}play.png`)
}
